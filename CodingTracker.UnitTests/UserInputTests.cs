using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace CodingTracker.UnitTests
{
    [TestClass]
    public class UserInputTests
    {
        [TestMethod]
        public void GetUserInputInt_ReturnedResult_Int()
        {
            //Arrange
            int input = new int();
            //var helper = new CodinSession(); //Stuck on assembly reference
            //helper.GetUserInputInt(input);

            //Act

            //input.GetUserInputInt(1);

            //Assert
        }
    }
}
